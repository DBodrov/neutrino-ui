import styled from '@emotion/styled';

export const StyledOptionsList = styled.div`
    display: flex;
    flex-flow: column nowrap;
    max-height: 20rem;
    overflow-y: auto;
`;

export const StyledNoData = styled.span`
    display: flex;
    flex-flow: row wrap;
    font-size: 14px;
    padding: 8px 4px;
    user-select: none;
    font-style: italic;
`;
