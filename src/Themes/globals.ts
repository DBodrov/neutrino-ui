export interface IGlobals {
    fontFamily?: string;
    fontSize?: string;
    borderRadius?: string;
}

export const globals: React.CSSProperties = {
    fontSize: '16px',
    borderRadius: '4px',
}
